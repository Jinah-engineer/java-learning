package p02.lecture;

public class A03Type {
	public static void main(String[] args) {
		// javascript 
		// var a = 3; --> number type
		// a = "hello"; --> string
		// a = true; // boolean
		
		// java에서는 변수를 선언할 때 타입을 선! 결정해준다. (선결정후할당)
		// 타입: 기본타입 and 참조타입 
		// 기본타입 : byte / short / int / long / boolean / char / short / double (8가지)
		// 참조타입 : 기본타입이 아닌 모든 타입
		
		int a;
		a = 3; // good
//		a = true; // bad - int에는 정수만!
	}
}
